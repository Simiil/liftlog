* charts with less than 3 sessions should still render at its size but empty content to keep the layout consistent
* session detail view looks weird (each line has rounded corners)
* i8n:
  * general UI (easy)
  * translations of the seeded exercises
